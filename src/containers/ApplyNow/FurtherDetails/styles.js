import { StyleSheet } from 'react-native';

import { APP_COLOR, THEME_CANADA } from '../../../constants';

export const styles = StyleSheet.create({
  themeAmerica: {
    color: APP_COLOR,
  },
  themeCanada: {
    color: THEME_CANADA,
  },
});

export default styles;
