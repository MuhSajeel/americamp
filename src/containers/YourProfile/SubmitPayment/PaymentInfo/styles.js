import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  bold: {
    fontWeight: 'bold',
    fontSize: 17,
    color: 'black',
  },
  para: {
    fontSize: 17,
    color: 'black',
    paddingTop: 10,
    paddingBottom: 10,
  },
});

export default styles;
