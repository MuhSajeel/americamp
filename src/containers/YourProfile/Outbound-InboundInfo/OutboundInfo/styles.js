import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  inputField: {
    borderColor: 'black',
    borderWidth: 1,
    marginTop: 10,
    marginBottom: 10,
    borderRadius: 5,
  },
});
