// /* eslint-disable camelcase */
// import { Stripe } from '@zubair998/stripe-api';
// import { STRIPE_API_KEY } from '../../constants';

// const stripe = new Stripe(STRIPE_API_KEY);

// export const createToken = async ({ card_number, expiryMonth, expiryYear, cvv }) => {
//   return stripe.createCardToken({ card_number, expiryMonth, expiryYear, cvv });
// };

// export const createCustomer = async ({ description, token }) => {
//   return stripe.createCustomerFromToken({ description, token });
// };
