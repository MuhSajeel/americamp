import { AMERICA_THEME, CANADA_THEME } from '../../constants';

export const americaTheme = () => {
  return { type: AMERICA_THEME };
};

export const canadaTheme = () => {
  return { type: CANADA_THEME };
};
